============
Installation
============

At the command line::

    $ easy_install django-approval

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-approval
    $ pip install django-approval
